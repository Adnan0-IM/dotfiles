# ghostty

Configuration for the Ghostty terminal emulator.

## Contents
- `.config/ghostty/`:
    - `config`: Main configuration file for terminal behavior, fonts, and window settings.
    - `themes/`: Directory containing custom color schemes and themes.
