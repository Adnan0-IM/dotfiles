# .config

Shared configuration files that are symlinked directly into the `~/.config` directory.

## Contents
- `starship.toml`: Configuration for the [Starship](https://starship.rs/) cross-shell prompt, defining its appearance and active modules.
